# .dockerignore

# Git stuff
.git
.gitignore

# Node modules (we run npm install inside the container)
server/node_modules
node_modules

# Local environment variables
server/.env
.env

# Operating System files
.DS_Store
Thumbs.db

# Build artifacts (if any)
server/build
server/dist

# Frontend code (not needed for backend image)
client/

# Docker files themselves
Dockerfile
.dockerignore

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*